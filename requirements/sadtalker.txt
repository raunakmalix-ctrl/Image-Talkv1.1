# venv_sadtalker — SadTalker talking head (isolated).
# Installed in addition to SadTalker's own requirements.txt (see make_venvs.sh).
# torch/torchvision/torchaudio are installed by make_venvs.sh (CUDA wheel index).
numpy<2
face-alignment==1.3.5
imageio==2.19.3
imageio-ffmpeg==0.4.7
librosa==0.10.0.post2
numba
resampy==0.3.1
pydub==0.25.1
scipy
kornia==0.6.8
yacs==0.1.8
pyyaml
gfpgan
safetensors
