# venv_voice — XTTS-v2 voice cloning (isolated; pins old transformers stack).
# coqui-tts is the maintained fork of the original (unmaintained) "TTS" package;
# it still exposes `import TTS`. torch/torchaudio are installed by make_venvs.sh
# from the CUDA wheel index.
#
# Use >=0.25: 0.24.x crashes loading the XTTS config on Python 3.10
# ("issubclass() arg 1 must be a class" in coqpit); the newer fork ships the
# fixed config library.
coqui-tts>=0.25.0
coqpit-config
# coqui-tts imports transformers.pytorch_utils.isin_mps_friendly, which
# transformers 5.x removed — pin to the 4.x line.
transformers>=4.43,<5
soundfile
numpy<2
# spacy (pulled by coqui-tts) imports these but they don't always resolve.
click
typer
